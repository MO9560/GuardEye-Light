# Android
*.iml
.gradle/
local.properties
.DS_Store/
/build/
/captures/

# Models (large binary, download separately)
*.tflite

# IDE
.idea/
*.ipr
*.iws
*.iml

# Logs
*.log
app/build/
local.properties

# Keep model files in repo (required for release build)
# *.tflite  <- commented out to include model in repo

# Temp build artifact
21

# Signing (contains secrets — generated locally / by GitHub Actions)
signing.properties
app/signing.properties
guard-eye.jks
guard-eye.keystore
guard-eye.b64
guard-eye-clean.b64
