<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions — Camera1 compatible, no Camera2/CameraX requirements -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required for android:foregroundServiceType="camera" on Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Camera1 only — no autofocus required -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuardEye"
        tools:targetApi="34">

        <!-- Status Activity — real-time service dashboard -->
        <activity
            android:name=".light.StatusActivity"
            android:exported="false"
            android:theme="@style/Theme.GuardEye" />

        <!-- Light Activity — simplified UI, no AI toggles -->
        <activity
            android:name=".light.LightMainActivity"
            android:exported="true"
            android:theme="@style/Theme.GuardEye">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Light Bot Service — Telegram polling + command routing -->
        <service
            android:name=".light.LightBotService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Camera Service — pure camera controller; bound by LightBotService -->
        <service
            android:name=".light.CameraForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- Light Alarm Receiver — triggers capture -->
        <receiver
            android:name=".light.LightAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot Receiver — restore on boot -->
        <receiver
            android:name=".light.LightBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
